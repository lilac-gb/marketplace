export const SortDirection = Object.freeze({
  ASK: 'ASC',
  DESC: 'DESC',
});

export const ModelParams = Object.freeze({
  CREATED_AT: 'created_at',
  VIEWS: 'views',
});

export const ModelStatuses = Object.freeze({
  STATUS_NOT_PUBLISHED: 0,
  STATUS_PUBLISHED: 1,
  STATUS_MODERATION: 2,
  STATUS_DONE: 1,
  STATUS_PROCESS: 0,
  STATUS_SHIPPING: 2,
});
