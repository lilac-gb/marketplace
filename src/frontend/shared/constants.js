export const SortDirection = Object.freeze({
  ASK: 'ASC',
  DESK: 'DESC',
});

export const NewsModel = Object.freeze({
  CREATED_AT: 'created_at',
  VIEWS: 'views',
});

export const NewsStatuses = Object.freeze({
  STATUS_NOT_PUBLISHED: 0,
  STATUS_PUBLISHED: 1,
  STATUS_MODERATION: 2,
});
